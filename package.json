{
  "name": "tourism",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "build:front": "cd frontend && yarn install && yarn build",
    "build:back": "cd backend && yarn instali && yarn build build",
    "build:copy": "cpx \"frontend/build/**\" backend/dist/build",
    "build": "yarn build:front && yarn build:back && yarn build:copy && rimraf node_modules",
    "start": "cd backend && yarn start:prod"
  },
  "dependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^3.0.2"
  }
}
