{
  "name": "tourism",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "build:front": "cd frontend && yarn install && yarn build",
    "build:back": "cd backend && yarn install && yarn build",
    "build:copy": "cpx \"frontend/build/**\" backend/dist/public",
    "build": "yarn build:front && yarn build:back && yarn build:copy",
    "start": "cd backend && yarn prod"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  }
}
